import React, { Component } from 'react';
import RegisterView from './registerUserView.js';

class RegisterClientView extends RegisterView {

    constructor(props) {
        super(props);
        // This component will be used to implement the specifics of registering a client
    }
}

export default RegisterClientView;